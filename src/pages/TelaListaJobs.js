import React from "react";

export default class TelaListaJobs extends React.Component{
    render(){
        return(
            <section>
                 Oi, eu sou tela de lista para jobs! :)
                {/* Tela para exibir a lista de jobs e opções para contratar o serviço */}
            </section>
        )
    }
}