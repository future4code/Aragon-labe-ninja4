import React from "react";

export default class TelaInicial extends React.Component{
    render(){
        return(
            <section>
                Oi, eu sou a tela inicial :)
                {/* HomePage, tela que o usuário irá cadastrar e contratar os jobs */}
            </section>
        )
    }
}